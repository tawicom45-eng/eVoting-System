"""
Dashboard app package initializer.

This file makes `dashboard` a regular package so test discovery
and imports work correctly (avoids namespace-package with no __file__).
"""

default_app_config = 'dashboard.apps.DashboardConfig'
